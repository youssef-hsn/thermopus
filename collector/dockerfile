FROM python:3.11-slim

WORKDIR /app
COPY app.py /app

# Keep it minimal: prometheus_client provides the HTTP server
RUN pip install --no-cache-dir prometheus_client

ENV PORT=8000
EXPOSE 8000

CMD ["python", "app.py"]